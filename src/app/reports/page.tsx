import { ReportsView } from "@/components/reports/ReportsView";

/** Route: "/reports" — analytics & stats dashboard. */
export default function ReportsPage(): JSX.Element {
  return <ReportsView />;
}
